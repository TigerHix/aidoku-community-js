{
  "name": "aidoku-community-js",
  "version": "1.0.0",
  "private": true,
  "description": "CI/CD for Aidoku community sources registries with author metadata",
  "scripts": {
    "fetch": "bun scripts/fetch-upstream.ts",
    "postprocess": "bun scripts/postprocess-registry.ts",
    "report": "bun scripts/generate-build-report.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
